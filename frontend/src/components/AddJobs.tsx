import React from 'react';
import { Job } from '../../../models/job';

interface IAddJobsProps {
    setJobs: (data: Job[]) => void
}

export const AddJobs: React.FC<IAddJobsProps> = ({}) => {
    return (
        <form>
            
        </form>
    );
};